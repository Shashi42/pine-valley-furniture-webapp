@model IEnumerable<S3G2_PVFAPP.Models.Production>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Product.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Work_center.WorkcenterName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Product.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Work_center.WorkcenterName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { productID = item.ProductID, workcenterID = item.WorkcenterID }) |
            @Html.ActionLink("Details", "Details", new { productID = item.ProductID, workcenterID = item.WorkcenterID }) |
            @Html.ActionLink("Delete", "Delete", new { productID = item.ProductID, workcenterID = item.WorkcenterID })
        </td>
    </tr>
}

</table>
