@model IEnumerable<S3G2_PVFAPP.Models.Vendor>

@{
    ViewBag.Title = "Index";
}
<link href="~/Content/TableDisplayStyles.css" rel="stylesheet" />
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.VendorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VendorAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VendorPostalcode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.VendorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VendorAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VendorPostalcode)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id = item.VendorID })" class="btn btn-dark">Edit</a>
            <a href="@Url.Action("Details", new { id = item.VendorID })" class="btn btn-primary">Details</a>
            <a href="@Url.Action("Delete", new { id = item.VendorID })" class="btn btn-danger">Delete</a>
        </td>
    </tr>
}

</table>
