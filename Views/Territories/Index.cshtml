@model IEnumerable<S3G2_PVFAPP.Models.Territory>

@{
    ViewBag.Title = "Index";
}
<link href="~/Content/TableDisplayStyles.css" rel="stylesheet" />
<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TerritoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TerritoryName)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id = item.TerritoryID })" class="btn btn-dark">Edit</a>
            <a href="@Url.Action("Details", new { id = item.TerritoryID })" class="btn btn-primary">Details</a>
            <a href="@Url.Action("Delete", new { id = item.TerritoryID })" class="btn btn-danger">Delete</a>
        </td>
    </tr>
}

</table>
