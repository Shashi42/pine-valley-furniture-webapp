@model IEnumerable<S3G2_PVFAPP.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<link href="~/Content/TableDisplayStyles.css" rel="stylesheet" />
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostalCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostalCode)
        </td>
        <td>
            <a href="@Url.Action("Edit", new { id = item.CustomerID })" class="btn btn-dark">Edit</a>
            <a href="@Url.Action("Details", new { id = item.CustomerID })" class="btn btn-primary">Details</a>
            <a href="@Url.Action("Delete", new { id = item.CustomerID })" class="btn btn-danger">Delete</a>
        </td>
    </tr>
}

</table>
